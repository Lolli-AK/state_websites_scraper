# http2 extra pulls in `h2`: several county sites 403 on HTTP/1.1 and 200 on h2
httpx[http2]>=0.27
beautifulsoup4>=4.12
lxml>=5.0
playwright>=1.44
# brotli: required because we advertise `br` in Accept-Encoding
brotli>=1.1
# openpyxl: scripts/export_xlsx.py generates the review workbook from targets.csv
openpyxl>=3.1
# pytest is only needed to run tests/, not to snapshot:
#   .venv/bin/pip install pytest

